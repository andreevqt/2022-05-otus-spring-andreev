spring:
  datasource:
    url: jdbc:h2:mem:test
    console:
      enabled: true
  sql:
    init: always
    data-locations: data.sql
  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true
